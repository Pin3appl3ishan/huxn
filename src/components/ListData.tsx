import React from "react";
import { View, Text,FlatList } from "react-native";

const ListData = () => {
    const fruits = ["aple", "bananaan", "nangl"]

  return (
    <View>
      <Text>List data</Text>
    </View>
  );
};

export default ListData;
